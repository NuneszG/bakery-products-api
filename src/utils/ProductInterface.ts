


export interface ProductInteger {
    id: string
    name: string
    price: number
    data: string
    type: string
    restant: number
    stockType: string
};

export interface ProductId {
    id: string
};

export interface ProductUpdated {
    id: string
    name: string
    price: number
    data: string
    restant: number
};

export interface ProductBody {
    name: string
    price: number
    data: string
    restant: number
    type: string
    stockType: string
};

